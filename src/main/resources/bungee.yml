name: ${project.artifactId}-bungee
main: ${project.groupId}.${project.artifactId}.bungee.MinecraftBridgeBungee
version: ${project.version}
author: xXSchrandXx
description: WSC-Minecraft-Bridge

commands:
  wscbridge:
    description: Command for WSC-Minecraft-Bridge.
    aliases:
    - wscbridgebungee
